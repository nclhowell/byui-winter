import { BikesFilterPipe } from './bikes-filter.pipe';

describe('BikesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BikesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
